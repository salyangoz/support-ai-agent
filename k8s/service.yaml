apiVersion: v1
kind: Service
metadata:
  name: support-ai-agent
  namespace: support-ai
  labels:
    app: support-ai-agent
spec:
  type: ClusterIP
  selector:
    app: support-ai-agent
    component: api
  ports:
    - port: 3001
      targetPort: 3001
      protocol: TCP
      name: http
