# ─── API Deployment ───
apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-ai-api
  namespace: support-ai
  labels:
    app: support-ai-agent
    component: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: support-ai-agent
      component: api
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: support-ai-agent
        component: api
    spec:
      containers:
        - name: api
          image: ghcr.io/salyangoz/support-ai-agent:latest
          command: ["node", "dist/index.js"]
          ports:
            - containerPort: 3001
              protocol: TCP
          envFrom:
            - configMapRef:
                name: support-ai-config
            - secretRef:
                name: support-ai-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
---
# ─── Worker Deployment ───
apiVersion: apps/v1
kind: Deployment
metadata:
  name: support-ai-worker
  namespace: support-ai
  labels:
    app: support-ai-agent
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: support-ai-agent
      component: worker
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: support-ai-agent
        component: worker
    spec:
      containers:
        - name: worker
          image: ghcr.io/salyangoz/support-ai-agent:latest
          command: ["node", "dist/worker.js"]
          envFrom:
            - configMapRef:
                name: support-ai-config
            - secretRef:
                name: support-ai-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
